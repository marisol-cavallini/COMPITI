#include<stdio.h>
int main(int argc, char*argv[])
{
    int a,b;
    double rapporto;
    do
    {
        printf("inserisci a");
        scanf("%d",&a);
        printf("inserisci b");
        scanf("%d",&b);
        if(a>b);
        {
           rapporto=(double)b/(double)a; 
        }else (b>a)
        rapporto=(double)a/(double)b;
        printf("resultato%f"R);
    }while(a!=0 && b!=0);
   return 0;
}
